{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx start-server",
    "start": "npx start-server -s ../frontend/dist"
  },
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}
